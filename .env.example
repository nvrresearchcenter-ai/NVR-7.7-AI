# Local development uses your own OpenAI key. On Netlify in production,
# AI Gateway auto-injects OPENAI_API_KEY and OPENAI_BASE_URL — no key required.
# To use AI Gateway locally, run the project with `netlify dev` after the site
# has at least one production deploy.
OPENAI_API_KEY=your_key_here
